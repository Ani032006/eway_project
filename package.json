{
  "name": "eway-intelligence",
  "private": true,
  "description": "E-Way Intelligence — run backend + React from this folder",
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "start:api": "npm run start --prefix backend",
    "build:web": "npm run build --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
